{
  "name": "check-diff-conflicts",
  "version": "1.0.0",
  "description": "checks diff between files and returns the file with the difference with comments to help solve the conflict",
  "main": "./build/index.js",
  "scripts": {
    "build": "npx tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "diff",
    "conflicts"
  ],
  "author": "Daniel Romero",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^20.2.1"
  }
}
